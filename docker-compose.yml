version: '3'
services:
  selenium:
    image: selenium/standalone-chromium:latest
    ports:
      - 4444:4444
      - 7900:7900
    restart: always
    shm_size: 2gb
    privileged: true
    environment:
      - SE_NODE_MAX_SESSIONS=2

  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:80
    env_file:
      - .env
    restart: always
    depends_on:
      - selenium

